/**
 * Configuration used by the Tocca library in order to change the behavior when using mobiles.
 */
var SWIPE_TRESHOLD = 1, // default value
    TAP_TRESHOLD = 1, // default value
    TAP_PRECISION = 1, // default value (touch events boundaries)
    JUST_ON_TOUCH_DEVICES = false; // default value ( decide whether you want to use the Tocca.js events only on the touch devices )
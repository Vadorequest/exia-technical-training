/**
 * bootstrapHelper
 *
 * @module      :: bootstrapHelper
 * @description :: Help to bootstrap the application by requiring all libraries and globals.
 */
module.exports = require('./../../shared/app/core/services/bootstrapHelper');

/**
 * Load modules that are specific to this application. Other modules that aren't required before can be loaded in the config/bootstrap file.
 */
